// Placeholder for the future GeoMind AI layer. No AI logic yet — these
// stubs exist so the rest of the app has a stable shape to build against
// once real AI assistance is introduced (Phase 4 of the product roadmap).

export async function suggestListingDescription(mineral, grade) {
  return null;
}

export async function explainMineralTerm(term) {
  return null;
}

export async function reviewListingForCompleteness(listing) {
  return { suggestions: [] };
}
